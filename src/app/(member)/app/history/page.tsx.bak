// src/app/(member)/app/history/page.tsx v1.1
//
// PURPOSE:
// - Member app history placeholder UI.
//
// CHANGES (v1.1):
// - FIX: Convert History page to light SaaS styling (readable dark text).
// - UX: Improve timeline card layout with subtle icon + better hierarchy.
// - Preserve "floating card" feel with soft shadows.
// - No backend wiring added (still placeholder only).
//
// VERSION HISTORY:
// - v1.0: Initial placeholder UI (dark/glass).
// - v1.1 (2026-03-01): Light theme + improved layout/UX.

const timeline = [
  { label: 'Quote drafted', detail: 'Kitchen refit quote saved', time: 'Today, 10:15' },
  { label: 'Invoice generated', detail: 'Invoice INV-0002 generated', time: 'Yesterday, 16:40' },
  { label: 'Lead updated', detail: 'Lead moved to quoted', time: 'Yesterday, 09:20' },
];

function DotIcon() {
  return (
    <span className="relative flex h-6 w-6 items-center justify-center">
      <span className="absolute inline-flex h-6 w-6 rounded-full bg-slate-200/70" />
      <span className="absolute inline-flex h-2.5 w-2.5 rounded-full bg-slate-700" />
    </span>
  );
}

export default function AppHistoryPage() {
  return (
    <div className="rounded-2xl border border-gray-200 bg-white p-5 shadow-[0_18px_50px_-35px_rgba(0,0,0,0.28)]">
      <div className="flex flex-wrap items-start justify-between gap-3">
        <div>
          <h2 className="text-lg font-semibold text-slate-900">Activity history</h2>
          <p className="mt-1 text-sm text-slate-600">
            Recent actions across quotes, invoices, leads, and jobs will appear here.
          </p>
        </div>

        <button
          type="button"
          disabled
          title="Coming soon"
          className="cursor-not-allowed rounded-full border border-gray-200 bg-white px-4 py-2 text-sm font-semibold text-slate-400 opacity-80"
        >
          Export (Coming soon)
        </button>
      </div>

      <div className="mt-5 space-y-3">
        {timeline.map((entry) => (
          <article
            key={`${entry.label}-${entry.time}`}
            className="rounded-2xl border border-gray-200 bg-white px-4 py-4 shadow-[0_16px_40px_-34px_rgba(0,0,0,0.22)]"
          >
            <div className="flex items-start justify-between gap-3">
              <div className="flex items-start gap-3">
                <div className="mt-0.5">
                  <DotIcon />
                </div>

                <div className="min-w-0">
                  <p className="font-semibold text-slate-900">{entry.label}</p>
                  <p className="mt-1 text-sm text-slate-600">{entry.detail}</p>
                </div>
              </div>

              <span className="shrink-0 rounded-full border border-gray-200 bg-gray-50 px-2.5 py-1 text-xs font-semibold text-slate-700">
                {entry.time}
              </span>
            </div>
          </article>
        ))}
      </div>

      <div className="mt-5 rounded-2xl border border-gray-200 bg-gray-50 px-4 py-4">
        <p className="text-sm font-semibold text-slate-900">Next</p>
        <p className="mt-1 text-sm text-slate-600">
          When we wire real history, this page will show Leads, Jobs, Quotes, Invoices, and Assets events
          (including support-mode actions).
        </p>
      </div>
    </div>
  );
}