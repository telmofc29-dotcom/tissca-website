# ====================================
# BUILDR - Phase A (Foundation) Setup
# ====================================
# Copy this file to .env.local and fill in actual values

# ====================================
# DATABASE (Supabase PostgreSQL)
# ====================================
# Get from Supabase dashboard: Settings > Database > Connection String
# Use the PostgreSQL connection string, not the URI connection string
DATABASE_URL="postgresql://postgres:password@db.supabase.co:5432/postgres?schema=public&sslmode=require"

# ====================================
# SUPABASE AUTHENTICATION
# ====================================
# REQUIRED: Get from Supabase dashboard: Settings > API
# These are exposed to the browser (ok - limited access)
NEXT_PUBLIC_SUPABASE_URL="https://your-project.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key-here"

# REQUIRED for server-side operations (sign out, admin tasks, etc)
# NEVER expose publicly - server-side only
# Get from Supabase dashboard: Settings > API > Service Role Secret
SUPABASE_SERVICE_ROLE_KEY="your-service-role-secret-here"

# Note: Missing these vars will cause 500 errors in API routes
# but will NOT block the build

# ====================================
# STRIPE (Payment & Subscriptions)
# ====================================
# Get from Stripe Dashboard
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_live_or_test_key"
STRIPE_SECRET_KEY="sk_live_or_test_key"

# Price IDs for subscriptions
NEXT_PUBLIC_STRIPE_PRICE_ID_MONTHLY="price_xxx_monthly"
NEXT_PUBLIC_STRIPE_PRICE_ID_ANNUAL="price_xxx_annual"

# Webhook signing secret (set after creating webhook endpoint)
STRIPE_WEBHOOK_SECRET="whsec_xxx"

# ====================================
# APPLICATION
# ====================================
NEXT_PUBLIC_BASE_URL="http://localhost:3000"
NODE_ENV="development"

# Admin emails (comma-separated for basic admin access)
ADMIN_EMAILS="admin@example.com,you@example.com"
